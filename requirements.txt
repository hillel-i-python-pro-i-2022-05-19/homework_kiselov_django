Django==4.0.6
django-phone-field==1.8.1
django-phonenumber-field
django-phonenumbers
django-environ==0.9.0
psycopg2-binary==2.9.3
faker==13.14.0


# [code_style]-[begin]
# pre-commit hook
pre-commit==2.19.0
# Upgrade syntax
pyupgrade==2.37.3

# Formatter. Apply codestyle
#для автоматического форматирования кода
# будет жёстко применять некоторые правила по стилям
black==22.6.0
# black --check . - посмотреть что можно изменить в проекте

# !!!!!некоторые такие инструменты игнорируют venv и их нужно явно указывать
# можно дополнительно сконфигурировать(google -> python black config)

# Нам нужен файл pyproject.toml ->> PEP 518 определяет pyproject.toml как конфигурационный файл для хранения системных
# требований сборки питоновский проектов ->>Используя Poetry or Flit он может заменить setup.py и setup.cfg файлы.

# Static analysis
# prospector собирает несколько инструментов в одно
prospector[with_everything]==1.7.7
# [code_style]-[end]






